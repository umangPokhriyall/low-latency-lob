[package]
name         = "bench"
version      = "0.1.0"
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true

[lints]
workspace = true

[dependencies]
book = { path = "../book" }
feed = { path = "../feed" }
sync = { path = "../sync" }
